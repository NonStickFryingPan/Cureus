{
  "name": "cureus",
  "version": "1.0.0",
  "private": true,
  "description": "Cureus — curated streaming reviews",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.39.0",
    "vite": "^6.3.2",
    "wrangler": "^4.95.0"
  }
}
